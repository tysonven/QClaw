---
name: ghl-kairos
category: on-demand
surface: both
keywords: [kairos, "kairos wines", vineyard, waitlist, "sponsor a vine", wine, "kairos contacts", "kairos leads"]
tools: [ghl_kairos__search_contacts, ghl_kairos__get_contact, ghl_kairos__list_opportunities, ghl_kairos__create_contact, ghl_kairos__update_contact, ghl_kairos__add_note, ghl_kairos__create_task, ghl_kairos__send_email_draft]
description: Kairos Wines GHL sub-account — contacts, waitlist, sponsor a vine leads, notes, tasks, email drafts
---

# GoHighLevel — Kairos Wines

<!--
  Replica of the ghl-fsc.md template pattern, scoped to the Kairos Wines
  sub-account GHL location (secrets.ghl_kairos_location_id). Swap of the
  per-brand secret key names (ghl_kairos_api_key / ghl_kairos_location_id)
  and the literal user ID — structure is otherwise identical to ghl-fsc.md
  / ghl-flowos.md / ghl-crete.md / ghl-sproutcode.md.

  No admin user exists scoped exclusively to the Kairos location (unlike
  Crete/SproutCode, which each have a dedicated single-location admin) —
  the three users returned by GET /users/?locationId=... are all
  multi-location (2 agency-wide, 1 Tyson personal). Tyson chose the
  Tyson Gmail account (narrowest scope of the three) as the literal user
  ID below.

  WRITES ENABLED. The skill HTTP write approval gate (PR #58) is live —
  ApprovalGate.check() gates every skill-defined POST/PUT/PATCH/DELETE behind
  Telegram approval at runtime (HTTP_WRITE_METHODS in
  src/security/approval-gate.js), so write endpoints here can no longer execute
  autonomously. The skill executor body-serialisation fix (PR #68) is live, so
  POST/PUT bodies (notes/tasks/contacts) serialise correctly. The frontmatter
  tools list is documentation only — actual tool names are auto-generated by
  skill-parser.js from method + path.
-->

## Auth
Base URL: https://services.leadconnectorhq.com
Header: Authorization: Bearer {{secrets.ghl_kairos_api_key}}
Header: Version: 2021-07-28
Header: Location-Id: {{secrets.ghl_kairos_location_id}}

## Endpoints
GET /contacts/?locationId={{secrets.ghl_kairos_location_id}}&query={{query}} - Search Kairos Wines contacts by name, email or phone
GET /contacts/{{contact_id}} - Get a single Kairos Wines contact by ID
GET /opportunities/search?location_id={{secrets.ghl_kairos_location_id}} - List Kairos Wines opportunities

# Write endpoints (all require Telegram approval via ApprovalGate)
POST /contacts/?locationId={{secrets.ghl_kairos_location_id}} - Create Kairos Wines contact. Body: {firstName, lastName, email, phone, locationId}. ALWAYS search by email first — dedup is mandatory.
PUT /contacts/{{contact_id}} - Update Kairos Wines contact fields. Body: any subset of {firstName, lastName, email, phone, tags, customFields}.
POST /contacts/{{contact_id}}/notes - Add internal note to Kairos Wines contact. Body: {body, userId}. userId: see Usage Notes for literal value.
POST /contacts/{{contact_id}}/tasks - Create task against Kairos Wines contact. Body: {title, body, dueDate, completed, contactId, assignedTo}. assignedTo: see Usage Notes for literal value.
POST /conversations/messages - Create email DRAFT to an existing Kairos Wines contact only. Body: {type: "Email", contactId, emailFrom, emailTo, emailSubject, emailBody, attachments: []}. NEVER send to arbitrary addresses — contactId is mandatory. This creates a draft, not a live send; Tyson reviews before sending.

## Permissions
- http: [services.leadconnectorhq.com]
- shell: none
- file: none

## Usage Notes
- Write operations: all write tools require Telegram approval before executing — enforced at runtime by ApprovalGate (PR #58 HTTP method discriminator on POST/PUT/PATCH/DELETE), not just a prompt instruction.
- ALWAYS search by email before creating a contact (search contacts first) — dedup is mandatory; never create a contact without a prior email search.
- Tyson's Kairos user ID is XawO37AiqDTKQfIKozky (Tyson Gmail, tyson.venables@gmail.com) — use this literal value as the assigned user (userId for notes, assignedTo for tasks). No admin user exists scoped exclusively to Kairos, so Tyson's personal account (narrowest-scoped of the available options) is used instead. Do not pass {{secrets.X}} syntax or env-var names as argument values.
- Location scoping is per-endpoint: contacts require the `locationId` query parameter (camelCase); opportunities require `location_id` (snake_case). The Location-Id header alone is not sufficient for these two endpoints.
- Email in GHL cannot be sent to arbitrary addresses — POST /conversations/messages requires a contactId, so a contact must exist first. This queues a DRAFT only; Tyson sends after review.
- No Kairos internal notify contact ID exists yet — do not fabricate one. Skip operator-notification notes until Tyson provides a contact ID for this purpose.
- When a tool argument requires a known ID value (contact_id, assignedTo etc.), use the literal value from Usage Notes — never pass {{secrets.X}} syntax as an argument value.
- British English in all notes and communications.
- This file is a per-brand replica of the ghl-fsc.md template — the Kairos Wines scope uses ghl_kairos_api_key / ghl_kairos_location_id; do not cross brands (FSC values live in ghl-fsc.md, Flow OS values live in ghl-flowos.md, Crete values live in ghl-crete.md, SproutCode values live in ghl-sproutcode.md).
