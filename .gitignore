# Dependencies
node_modules/

# Environment (secrets live here)
.env
.env.local
.env.*.local

# Encrypted secrets and databases
*.enc
*.db
*.pid

# QuantumClaw runtime
.quantumclaw/
workspace/agents/*/memory/
workspace/memory/
workspace/logs/
workspace/delivery-queue/

# AGEX credentials (private key is in encrypted secrets store)
workspace/agex/

# Logs
*.log

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Docker volumes
docker-data/

# Backup files
*.backup
*.backup[0-9]
*.bak
*.bak[0-9]
*.tmp
*.pre-fix

# Face detection model (too large for git, download on deploy)
src/clipper/models/*.caffemodel

# Private keys, never commit
*.pem
*.key
*_key
*_rsa
*_ed25519
id_*
**/charlie_n8n_key

# Charlie dispatch transient briefs (recurring; narrow ignore — CC_OPERATING_RULES.md and CC_INVENTORY.md remain tracked)
CLAUDE_CODE_TASKS.md

# Timestamped backup files (qclaw-dev skill convention: file.bak.<timestamp> / file.backup.<epoch>)
*.bak.*
*.backup.*
